[package]
name = "passport-platform"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]
doctest = false

[dependencies]
passport-core = { path = "../passport-core" }
